[project]
name = "claude-org-runtime"
dynamic = ["version"]
description = "Python runtime for claude-org-ja: dispatcher and state schema."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "suisya-systems", email = "iwama.ryo.0731@gmail.com" }]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "License :: OSI Approved :: MIT License",
]
dependencies = [
  "jsonschema>=4.18",
]

[project.optional-dependencies]
dev = ["pytest"]

[project.urls]
Repository = "https://github.com/suisya-systems/claude-org-runtime"
Issues = "https://github.com/suisya-systems/claude-org-runtime/issues"

[project.scripts]
claude-org-runtime = "claude_org_runtime.cli:main"

[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.dynamic]
version = { attr = "claude_org_runtime.__about__.__version__" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"claude_org_runtime.settings" = ["*.json"]
"claude_org_runtime.fencing" = ["*.json"]
"claude_org_runtime.control_plane" = ["*.sql"]
# The production DDL ledger travels with the package: without this the wheel
# ships a migrator whose steps are missing, which every opener then refuses.
"claude_org_runtime.control_plane.migrations" = ["*.sql"]
"claude_org_runtime.canary" = ["*.sql"]
